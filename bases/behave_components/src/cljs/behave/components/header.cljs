(ns behave.components.header)

(defn h1 [text] [:h1 text])

(defn h2 [text] [:h2 text])

(defn h3 [text] [:h3 text])

(defn h4 [text] [:h4 text])

(defn h5 [text] [:h5 text])

(defn h6 [text] [:h6 text])
