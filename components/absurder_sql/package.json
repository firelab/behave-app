{
  "name": "absurder-sql",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "shadow-cljs": "2.20.5"
  },
  "dependencies": {
    "@npiesco/absurder-sql": "^0.1.23"
  },
  "scripts": {
    "test:karma": "bun run shadow-cljs compile test && bun karma start --log-level debug",
    "compile:pss": "rm -rf build && bun build.ts && cp ./build/* ./src/persistent_sorted_set_js/"
  }
}
